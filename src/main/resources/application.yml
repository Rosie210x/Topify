spring:
  datasource:
    url: jdbc:mysql://localhost:3306/topify?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: ChangeThisSecretForProductionChangeThisSecretFor
  expiration-ms: 86400000
